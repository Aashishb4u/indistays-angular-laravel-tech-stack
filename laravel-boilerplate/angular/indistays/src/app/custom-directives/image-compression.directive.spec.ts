import { ImageCompressionDirective } from './image-compression.directive';

describe('ImageCompressionDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCompressionDirective();
    expect(directive).toBeTruthy();
  });
});
